services:
  - type: web
    name: agri-query-engine
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python whisper_main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
